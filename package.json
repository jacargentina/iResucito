{
  "private": true,
  "workspaces": {
    "packages": [
      "./packages/*"
    ]
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "reset": "find . -type dir -name node_modules | xargs rm -rf && npm install",
    "web": "npm run build && cd packages/web && npm run dev"
  },
  "dependencies": {
    "react-native-cli": "^2.0.1"
  }
}
