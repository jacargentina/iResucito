//import { } from '../actions';

import { Map } from 'immutable';

const initialState = Map({
});

export default function ui(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
